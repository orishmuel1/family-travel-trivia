# Normalize line endings so the repo is consistent across macOS, Windows, and Linux.
* text=auto eol=lf

# Binary assets — never apply line-ending conversion.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
